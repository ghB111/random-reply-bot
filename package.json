{
  "name": "random-reply-bot",
  "version": "1.0.0",
  "description": "A tg bot that replies with a random line from your file",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node ./build/app.js"
  },
  "author": "Roman Brek",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.54.0"
  },
  "devDependencies": {
    "@types/node": "^16.4.6",
    "nodemon": "^2.0.12",
    "rimraf": "^3.0.2",
    "ts-node": "^10.1.0",
    "typescript": "^4.3.5"
  }
}
